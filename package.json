{
  "name": "@harness-system/pb-agent-sync",
  "version": "1.1.0",
  "description": "CLI for synchronizing AI agent rules and skills.",
  "keywords": [
    "ai",
    "agents",
    "cli",
    "rules",
    "skills"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SanAdreas14745/agent-sync.git"
  },
  "bugs": {
    "url": "https://github.com/SanAdreas14745/agent-sync/issues"
  },
  "homepage": "https://github.com/SanAdreas14745/agent-sync#readme",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "bin": {
    "ai-skills": "dist/cli.js"
  },
  "files": [
    "dist",
    "registry",
    "README.md",
    ".npmrc.example"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "tsc",
    "test": "npm run build && node tests/composer.test.js && node tests/codex-adapter.test.js && node tests/agent-adapters.test.js && node tests/project-config.test.js && node tests/registry-lock.test.js && node tests/registry-provider.test.js && node tests/git-registry-provider.test.js && node tests/release-registry-provider.test.js && node tests/registry-review.test.js && node tests/cli.test.js && node tests/package-distribution.test.js",
    "prepack": "npm run build",
    "pack:check": "npm pack --dry-run",
    "verify": "npm run test && npm run pack:check",
    "prepublishOnly": "npm run verify",
    "ai-skills": "node dist/cli.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
