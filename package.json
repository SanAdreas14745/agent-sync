{
  "name": "@harness-system/pb-agent-sync",
  "version": "1.0.0",
  "description": "CLI for synchronizing AI agent rules and skills.",
  "main": "dist/index.js",
  "bin": {
    "ai-skills": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    ".npmrc.example"
  ],
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "tsc",
    "test": "npm run build && node tests/composer.test.js && node tests/codex-adapter.test.js && node tests/project-config.test.js && node tests/registry-provider.test.js && node tests/release-registry-provider.test.js && node tests/registry-review.test.js && node tests/cli.test.js",
    "ai-skills": "node dist/cli.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "private": true
}
